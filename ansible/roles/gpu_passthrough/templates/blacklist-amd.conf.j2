# Blacklist AMD drivers for GPU passthrough
# Managed by Ansible - gpu_passthrough role
# This prevents the host from claiming AMD GPUs
{% for driver in gpu_driver_blacklist.amd %}
blacklist {{ driver }}
{% endfor %}

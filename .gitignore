# Private keys and certificates
*.pem
*.key
*.pfx
*.p12
id_rsa*
id_ed25519
id_ecdsa

# Environment files
.env
.env.*

# AWS credentials
.aws/credentials

# SSH agent sockets
ssh-agent.*

# Tool caches
# ansible-lint stages mocked collections here on every run
.ansible/

# OS files
.DS_Store
Thumbs.db

# Local agent usage archives (Claude Code / Codex usage snapshots). Live outside the repo; never commit.
agent-usage/
history.ndjson
